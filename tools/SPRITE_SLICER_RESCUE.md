# Sprite slicer rescue note

`tools/sprite_slicer.py` was rescued from stale PR #7 (`feat-sprite-slicer-tool-12372363286373239396`) after the stale PR was found to be based on an unrelated-history `main` branch with mass asset/config deletion risk.

This branch brings forward only the tool into the current `master` lineage.
